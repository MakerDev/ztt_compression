name: ablation
do_train: true
do_eval: true
output_root: runs
batch_size: 2
gradient_accumulation_steps: 8
learning_rate: 2e-4
num_epochs: 0.5
max_seq_length: 1024
warmup_steps: 20
max_steps: 1000
logging_steps: 25
save_steps: 200
evaluation_strategy: steps
dataset:
  name: mmlu_aux
  subset_ratio: 0.1
  eval_name: mmlu
  eval_subset_ratio: 0.05
  text_field: text
